import type { TextRange } from '../types';
export declare const createFixer: () => {
    insertTextAt: (index: number, text: string) => {
        range: number[];
        text: string;
    };
    insertTextAfterRange: (range: TextRange, text: string) => {
        range: number[];
        text: string;
    };
    insertTextBeforeRange: (range: TextRange, text: string) => {
        range: number[];
        text: string;
    };
    replaceTextRange: (range: TextRange, text: string) => {
        range: TextRange;
        text: string;
    };
    removeRange: (range: TextRange) => {
        range: TextRange;
        text: string;
    };
};
