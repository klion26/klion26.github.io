export const getTextNodes = (node) => {
    const textNodes = [];
    const textOnlyNode = ['text', 'inlineCode'];
    if (textOnlyNode.includes(node.type)) {
        textNodes.push(node);
        return textNodes;
    }
    if (node.children) {
        const childNodes = node.children.reduce((prev, current) => {
            return prev.concat(getTextNodes(current));
        }, []);
        textNodes.push(...childNodes);
    }
    return textNodes;
};
//# sourceMappingURL=get-text-nodes.js.map